version: "3.8"
services:
  rinstat:
    # build: .
    image: istride/r-instat:0.1.0
    environment:
      DISPLAY: ${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ~/.Xauthority:/home/rinstat/.Xauthority
    network_mode: host
    command:
      - mono
      - instat.exe
